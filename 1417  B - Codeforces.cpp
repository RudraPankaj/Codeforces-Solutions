#include <bits/stdc++.h>
using namespace std;

#define ll long long int
#define nl "\n"
#define fast ios_base::sync_with_stdio(false); cin.tie(NULL);

int main() {
    fast;
    int t, n, i;

    while(t--) {
        cin >> n;
        int a[n+4];
        if(n==1) {
            cin >> a[n];
            cout << a[n] << nl;
        }
        else {
            for(i=1; i<=n; i++){
                cin >> a[i];
            }
            for(i=a.size(); i>1; i++) {

            }
        }
    }

}
